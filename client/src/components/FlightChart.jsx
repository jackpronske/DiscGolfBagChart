import React from "react";

const FlightChart = (props) => {
  return (
    <div className="flight-chart">
      {/* map through all the discs to apply a disc component in the correct grid area */}
    </div>
  );
};

export default FlightChart;
